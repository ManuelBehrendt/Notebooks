{
 "result": {
  "blueprint": {
   "chapters": [
    {
     "n": 0,
     "title": "Off-axis projection \u2014 what this page gives you",
     "objective": "Reader knows the scope, the one-line formula, the five knobs, and what they need to know already.",
     "cells": [
      {
       "kind": "markdown",
       "content": "Title + scope card. Prose:\n\n\"Every map on this page comes out of **one function call**:\n`projection(obj, var, unit; <view>, <framing>, <binning>, <pxsize>)`.\nOff-axis adds exactly one thing to the [axis-aligned page](06_hydro_Projection.md): **orientation**. Everything you already know \u2014 `center`, `range_unit`, `pxsize`, `weighting`, units \u2014 works unchanged.\"\n\n**Five knobs** table (one row each, no elaboration \u2014 each gets a chapter):\n| knob | keywords | chapter |\n|---|---|---|\n| view | `direction` \u00b7 `inclination`/`azimuth`/`axis` \u00b7 `los` \u00b7 `theta`/`phi` | 1, 3 |\n| framing | `fov`/`fov_unit`/`aperture` \u00b7 `xrange`/`yrange`/`zrange`+`center` | 4 |\n| quantity | `var`/`unit` lists \u00b7 `weighting` | 5 |\n| placement | `binning` (the default is already the accurate one) | 6 |\n| output | `m.maps`, `getextent`, camera metadata | 1, 10 |\n\n**Contract line, stated up front and honestly:** one dataset (`RAMSES/spiral_clumps` output 100, ~590 k cells, levels 5\u20137, 100 kpc box), 13 code cells, 20 hydro `projection` calls, 9 figures. Measured end-to-end wall clock at `julia -t 8`: **<FILL IN \u2014 measure before writing prose>**.\n\nPointer: full limitations table is Appendix A; nothing on this page is a forward reference to it."
      }
     ]
    },
    {
     "n": 1,
     "title": "Your first off-axis view",
     "objective": "Reader can turn a galaxy face-on and edge-on with one keyword, and can read the map and the camera off the returned object.",
     "cells": [
      {
       "kind": "markdown",
       "content": "\"A projection is a camera. The data never moves \u2014 only the camera does. Two keywords prove it.\"\n\nThen, BEFORE the code, the first of the three admitted inline caveats:\n\n!!! warning \"`direction=:faceon` needs `center` on the object\"\n    `:faceon`/`:edgeon` (and `axis=:angmom`) derive the orientation from the **angular momentum of the loaded data about `center`**. `center` defaults to `[0.,0.,0.]` \u2014 the box **corner** \u2014 and L about a corner is dominated by the lever arm of the whole box. You get a plausible-looking tilted galaxy, no error and no warning. Always pass `center=[:bc]` (or the object's own centre). Detection: edge-on, `:vlos` must be antisymmetric about the minor axis (Chapter 7)."
      },
      {
       "kind": "code",
       "content": "using Mera, CairoMakie, Statistics\nCairoMakie.activate!(type=\"png\")\n\ninfo = getinfo(100, joinpath(ENV[\"MERA_TEST_DATA\"], \"RAMSES/spiral_clumps\"), verbose=false)\ngas  = gethydro(info, verbose=false, show_progress=false)\n\nprintln(\"cells loaded : \", length(gas.data))\nprintln(\"box length   : \", info.boxlen, \" kpc\")\nprintln(\"levels       : \", gas.lmin, \" \u2013 \", gas.lmax)\nprintln(\"threads      : \", Threads.nthreads())"
      },
      {
       "kind": "code",
       "content": "# \u2500\u2500 figure code from here: panels, overlays, colorbars \u2014 no new Mera concepts \u2500\u2500\n# ONE helper, used by every figure in this notebook. It encodes the rules that make a\n# comparison honest: physical axes in kpc, DataAspect, no interpolation, an explicit\n# NaN colour, and an optional SHARED colour range.\nfunction showmap!(ax, m, key; logscale=true, cmap=:inferno, crange=nothing)\n    A = Float64.(m.maps[key])\n    A = logscale ? log10.(replace(A, 0.0 => NaN)) : A\n    e = getextent(m, :kpc)\n    xs = range(e[1], e[2], length=size(A,1))\n    ys = range(e[3], e[4], length=size(A,2))\n    hm = heatmap!(ax, xs, ys, A; colormap=cmap, nan_color=:black, interpolate=false,\n                  colorrange = crange === nothing ? Makie.automatic : crange)\n    ax.aspect = DataAspect()\n    return hm\nend\n\n# shared colour range across a set of maps (2nd percentile \u2192 max), so panels are comparable\nfunction sharedrange(ms, key; logscale=true)\n    v = reduce(vcat, [vec(Float64.(m.maps[key])) for m in ms])\n    v = logscale ? log10.(filter(x -> x > 0, v)) : filter(isfinite, v)\n    (quantile(v, 0.02), maximum(v))\nend\n\nfunction maprow(ms, key, titles; clabel=\"log10 \u03a3  [M\u2299/pc\u00b2]\", cmap=:inferno,\n                crange=nothing, logscale=true)\n    n = length(ms); fig = Figure(size=(330n + 110, 380)); hm = nothing\n    for i in 1:n\n        ax = Axis(fig[1,i], title=titles[i], xlabel=\"x' [kpc]\",\n                  ylabel = i == 1 ? \"y' [kpc]\" : \"\")\n        i > 1 && hideydecorations!(ax, grid=false)\n        hm = showmap!(ax, ms[i], key; logscale=logscale, cmap=cmap, crange=crange)\n    end\n    Colorbar(fig[1, n+1], hm, label=clabel)\n    fig\nend"
      },
      {
       "kind": "code",
       "content": "# ONE window, reused by every chapter \u2014 so the only thing that changes between cells\n# is the keyword being taught.\nwin = (center=[:bc], fov=22, fov_unit=:kpc, aperture=:square,\n       pxsize=[0.3, :kpc], verbose=false, show_progress=false)\n\nfo = projection(gas, :sd, :Msol_pc2; direction=:faceon, win...)\neo = projection(gas, :sd, :Msol_pc2; direction=:edgeon, win...)\n\nprintln(\"line of sight  \u0175 = \", round.(fo.los, digits=3))\nprintln(\"image up       \u00fb = \", round.(fo.up,  digits=3))\nprintln(\"projection centre (box fraction) = \", round.(fo.center, digits=3))\nprintln(\"frame: \", size(fo.maps[:sd]), \"  and  \", size(eo.maps[:sd]))\n\ncr = sharedrange([fo, eo], :sd)\nmaprow([fo, eo], :sd, [\"direction=:faceon\", \"direction=:edgeon\"]; crange=cr)",
       "figure": "FIG 1. Two panels, one keyword changed. Shared log colour scale, physical kpc axes, identical frame size printed above. This is the hook: same 590 311 cells, no reload, no re-gridding \u2014 only the camera moved. It also silently establishes that the frame is identical at both angles, which Chapter 4 will explain."
      },
      {
       "kind": "markdown",
       "content": "Read the output back:\n\n- `m.maps` is a dict of 2-D arrays; `m.maps[:sd]` is the surface density in `Msol_pc2`.\n- `getextent(m, :kpc)` returns `[xmin, xmax, ymin, ymax]` **in the image plane** \u2014 the raw `m.extent` is in code units, so always convert.\n- `m.los`, `m.up`, `m.cam_right` are the camera basis the call actually used; `m.direction` reads `:offaxis`.\n\nThat `los = [0, 0, 1]`-ish vector is not a coincidence you chose \u2014 it is the disc's angular momentum axis, computed from your data about your `center`. That is what makes `:faceon` a *measurement*, not a guess."
      }
     ]
    },
    {
     "n": 2,
     "title": "What a pixel contains",
     "objective": "Reader can state that a pixel is a line integral along parallel rays, that there is no perspective, and therefore that framing must be a field of view rather than a camera distance.",
     "cells": [
      {
       "kind": "markdown",
       "content": "Six sentences + the schematic. Content, no code:\n\n1. The camera is **orthographic**: all rays are parallel, the observer sits at infinity. There is no vanishing point and no perspective.\n2. Mera reduces any view specifier to one unit line-of-sight vector `\u0175` and completes it to a right-handed orthonormal camera basis `(r\u0302, \u00fb, \u0175)` \u2014 image x, image y, viewing direction (`m.cam_right`, `m.up`, `m.los`).\n3. `\u0175` points **into** the image, away from the observer. (Remember this: Chapter 7 turns it into a sign.)\n4. A pixel value is the integral of the requested quantity along the parallel ray through that pixel, over the whole depth of the selected data.\n5. `position_angle` is a **roll**: it rotates `(r\u0302, \u00fb)` together about `\u0175`. It changes how the image is oriented on the page, never what is in front of the camera.\n6. **Consequence that matters for the next chapter:** because the projection is orthographic, moving the camera away changes nothing. The only control over what is in frame is the *width of the frame* \u2014 a field of view. There is no camera distance to set.\n\nOne-line disposition note (honest, prevents an appendix landfill): the step-by-step basis construction (deterministic auto-`up`, Gram\u2013Schmidt, the roll matrix) lives in `?projection` and on the [API page](api/offaxis.md); it is not needed to use any of this."
      },
      {
       "kind": "markdown",
       "content": "![Off-axis camera geometry](assets/offaxis/offaxis_geometry_v2.svg)\n\n*Parallel rays, the image plane, and the camera basis `(r\u0302, \u00fb, \u0175)` planted at `center`. Left inset: `inclination` is measured from a **reference axis** \u2014 either the box `z` or the object's own angular-momentum axis `L`. They are not the same axis, and choosing the wrong one is the most common way to get a picture that looks right but is not.*",
       "figure": "FIG 2 (schematic, REDRAW of the existing offaxis_geometry.svg). Teaches: orthographic parallel rays (drawn with NO vanishing point), the three orthogonal basis vectors, the inclination and azimuth arcs, the roll arrow about \u0175. Plus one small two-element inset showing box-z and L visibly non-parallel. Cut from the current asset: the keyword sidebar (that is a table, and it is Chapter 3's table)."
      }
     ]
    },
    {
     "n": 3,
     "title": "Choosing the view",
     "objective": "Reader can pick a view specifier from observer language, knows the exactly-one rule, and knows what reference axis each specifier uses.",
     "cells": [
      {
       "kind": "markdown",
       "content": "**Observer vocabulary \u2192 Mera keywords** (the primary artefact of this chapter):\n\n| you want | you say | Mera |\n|---|---|---|\n| a disc seen flat / on its side | face-on / edge-on | `direction=:faceon` / `:edgeon` |\n| a specific inclination *i* | i = 60\u00b0 | `inclination=60, axis=:angmom` |\n| a specific position angle on the sky | PA = 30\u00b0 | `position_angle=30` |\n| a known viewing vector | \u0175 = (1,1,1) | `los=[1,1,1]` |\n| spherical camera angles | \u03b8, \u03c6 | `theta=`, `phi=` |\n| spin the object about its own axis | *(no observational counterpart)* | `azimuth=` |\n\n`azimuth` rotates the object about its own spin axis. For an axisymmetric disc it is unobservable \u2014 it is a *movie* parameter, not a modelling one, which is why it earns no static panel here and reappears in Chapter 9.\n\n**Reference axis.** `axis=:z` (default) tilts away from the box z-axis; `axis=:angmom` tilts away from the disc's own L, computed about `center`. `axis=[vx,vy,vz]` takes an explicit axis. `direction=:faceon/:edgeon` already imply `:angmom`, so combining them with `axis=` is an `ArgumentError`.\n\n**Exactly one view specifier.** `los` | `inclination`/`azimuth` | `theta`/`phi` | `direction=:faceon/:edgeon` are mutually exclusive; giving two raises immediately. `up=` and `position_angle=` are *modifiers* and combine with any of them.\n\n**Angles are degrees by default** (`angle_unit=:rad` to switch)."
      },
      {
       "kind": "code",
       "content": "# Same window as Chapter 1 \u2014 only `inclination` changes.\nlad0  = projection(gas, :sd, :Msol_pc2; inclination=0,  axis=:angmom, win...)\nlad30 = projection(gas, :sd, :Msol_pc2; inclination=30, axis=:angmom, win...)\nlad60 = projection(gas, :sd, :Msol_pc2; inclination=60, axis=:angmom, win...)\n\n# `direction=:faceon` is exactly `inclination=0, axis=:angmom` \u2014 not approximately:\nprintln(\"max |faceon \u2212 inclination=0|  =  \",\n        maximum(abs, lad0.maps[:sd] .- fo.maps[:sd]))\n\nladder = [lad0, lad30, lad60, eo]      # i = 90 is the edge-on map from Chapter 1\ncr = sharedrange(ladder, :sd)\nmaprow(ladder, :sd, [\"i = 0\u00b0\", \"i = 30\u00b0\", \"i = 60\u00b0\", \"i = 90\u00b0\"]; crange=cr)",
       "figure": "FIG 3. One row, four inclinations, one shared colour scale, one shared colourbar, kpc axes on the left panel. Teaches the monotonic flattening of a clumpy spiral into a thin lane \u2014 the one thing prose cannot do. Panels 1 and 4 are the Chapter 1 opener seen in sequence, which is the point: the shortcut is a ladder rung."
      },
      {
       "kind": "markdown",
       "content": "**So:** pick `direction=:faceon/:edgeon` when you want the disc's own frame, `inclination`+`axis=:angmom` when you want a specific *i*, and `los=` when you already know the vector \u2014 for example when you want the same orientation across many snapshots (Appendix C)."
      }
     ]
    },
    {
     "n": 4,
     "title": "Framing: world ranges versus camera field of view",
     "objective": "Reader can produce frames that are comparable across angles, and can recognise the grown-window artefact in their own maps.",
     "cells": [
      {
       "kind": "markdown",
       "content": "This is the surprise that catches everyone, so take the problem first.\n\n`xrange`, `yrange`, `zrange` (with `center` and `range_unit`) are **world-space** bounds \u2014 they select a box inside the simulation, exactly as they do for the axis-aligned path and for `subregion`. The camera frame is then auto-fitted to the **bounding box of that region after rotation**. Two consequences:\n\n1. **The frame grows with tilt.** A \u00b122 kpc world window on a 100 kpc box comes out \u00b155 kpc at *i* = 60\u00b0. Frames at different angles are not comparable, and the object appears to zoom.\n2. **The window's own faces appear in the map** as straight edges cutting across the image. Worse than cosmetic: with no `zrange` the *entire box depth* of foreground and background gas sits in your disc map, so any column, scale height or \u03a3 measured from it is contaminated.\n\nThere is **no line-of-sight depth slab in `projection`.** `zrange` clips world *z*; it coincides with depth only when the line of sight is near \u00b1z \u2014 precisely the face-on case where you need it least. (Also: an axis whose requested range already covers the loaded data's range is not clipped at all, so `zrange` on already-subregioned data can silently do nothing.)\n\n**The fix: `fov`.** Because the camera is orthographic (Chapter 2), the only framing control is the width of the frame. `fov` selects a **sphere** of radius `fov` about `center` \u2014 and a sphere projects to the same disc at every orientation, so the frame is fixed by construction.\n\n| `aperture` | selection | frame |\n|---|---|---|\n| `:circle` (default) | sphere of radius `fov` | rectangular array with **empty corners** |\n| `:square` | sphere of radius \u221a2\u00b7`fov`, cropped | **full rectangle, pixel-identical size and scale at every angle** |\n\nUse `:square` whenever you will compare or animate frames."
      },
      {
       "kind": "markdown",
       "content": "!!! warning \"`fov` always needs `fov_unit`\"\n    `fov_unit` defaults to `:standard`, which is a **box fraction**. `fov=22` alone means 22 box lengths, silently clamped to 0.49\u00b7boxlen. Always write `fov=22, fov_unit=:kpc`.\n\n    Three more things `fov` does: it **replaces** any `xrange`/`yrange`/`zrange` you pass; it makes `center` be read in `fov_unit` (your `range_unit` is discarded); and it **cannot be combined with a per-cell `mask`** (the sphere selection changes the cell count, so the mask no longer matches).\n\n    Caps: `:circle` at 0.49\u00b7boxlen, `:square` at 0.49/\u221a2\u00b7boxlen (\u2248 34.6 kpc on this fixture)."
      },
      {
       "kind": "code",
       "content": "# WORLD-space window \u2014 verbose=true so Mera's own hint about this is on the page.\nworld = projection(gas, :sd, :Msol_pc2; inclination=60, axis=:angmom, center=[:bc],\n                   xrange=[-22,22], yrange=[-22,22], range_unit=:kpc,\n                   pxsize=[0.3,:kpc], verbose=true, show_progress=false)\n\n# CAMERA-plane frame \u2014 rotation-invariant sphere selection.\nsq = projection(gas, :sd, :Msol_pc2; inclination=60, axis=:angmom, center=[:bc],\n                fov=22, fov_unit=:kpc, aperture=:square,\n                pxsize=[0.3,:kpc], verbose=false, show_progress=false)\nci = projection(gas, :sd, :Msol_pc2; inclination=60, axis=:angmom, center=[:bc],\n                fov=22, fov_unit=:kpc, aperture=:circle,\n                pxsize=[0.3,:kpc], verbose=false, show_progress=false)\n\nfor (nm, m) in ((\"xrange/yrange \u00b122 kpc\", world), (\"fov=22 :square\", sq), (\"fov=22 :circle\", ci))\n    println(rpad(nm, 24), \" frame \", lpad(string(size(m.maps[:sd])), 12),\n            \"   extent [kpc] = \", round.(getextent(m, :kpc), digits=1))\nend"
      },
      {
       "kind": "code",
       "content": "# \u2500\u2500 figure code from here: panels, overlays, colorbars \u2014 no new Mera concepts \u2500\u2500\ncr = sharedrange([world, sq, ci], :sd)\nttl = [\"xrange/yrange = \u00b122 kpc\\n\" * string(size(world.maps[:sd])),\n       \"fov=22, aperture=:square\\n\"  * string(size(sq.maps[:sd])),\n       \"fov=22, aperture=:circle\\n\"  * string(size(ci.maps[:sd]))]\nfig = maprow([world, sq, ci], :sd, ttl; crange=cr)\n# draw the REQUESTED \u00b122 kpc window on panel 1, so request and result are both visible\nlines!(contents(fig[1,1])[1], [-22,22,22,-22,-22], [-22,-22,22,22,-22],\n       color=:cyan, linestyle=:dash, linewidth=2)\nfig",
       "figure": "FIG 4. The highest-value new figure. Three panels at fixed i=60\u00b0, same pxsize, ONE shared colour scale and colourbar, array size printed in every panel title. Panel 1 shows the requested \u00b122 kpc box as a dashed cyan overlay sitting inside a frame that is actually \u00b155 kpc, with the box's own faces visible as straight edges across the map. Panels 2 and 3 are the same physical size as each other and as the i=0\u00b0 map from Chapter 1. Retires the old orbit_fov.png and orbit_aperture.png."
      },
      {
       "kind": "markdown",
       "content": "**So:** world ranges select *data*; `fov` frames the *camera*. If your frames must be comparable \u2014 across angles, across snapshots, or as movie frames \u2014 use `fov` + `fov_unit` + `aperture=:square`."
      }
     ]
    },
    {
     "n": 5,
     "title": "What you can ask for, and what the number means",
     "objective": "Reader can request several maps in one call and can say, for each one, whether it is a sum or a weighted mean \u2014 and therefore whether it is a budget or a picture.",
     "cells": [
      {
       "kind": "markdown",
       "content": "One call returns as many maps as you ask for. But the maps are not all the same *kind* of number, and this is where wrong values actually get published.\n\n| kind | examples | what a pixel is | safe to sum? |\n|---|---|---|---|\n| **summed (extensive)** | `:mass` | the total in that column | **yes** \u2014 this is a budget |\n| **per-area (extensive, divided)** | `:sd` | mass \u00f7 pixel area | no \u2014 `sum(:sd)` is not a mass; multiply by `pixsize\u00b2` in the right units first, or just use `:mass` |\n| **weighted mean (intensive)** | `:T`, `:vx`, `:vlos`, `:\u03c3los` | \u03a3(field\u00b7w) / \u03a3w along the ray | no |\n\n`weighting` is an **Array** for hydro/RT \u2014 `weighting=[:mass, missing]` (the default) means \"mass-weight the first variable, leave the second unweighted\". (For particles it is a plain `Symbol`; see Chapter 11.)\n\n**A weighted mean is not an observable.** A mass-weighted projected `T` along a multiphase sightline is dominated by cold dense gas; `weighting=[:volume]` gives a different number again; neither is the emission-weighted temperature an X-ray observation would report. Quote projected means as *diagnostics*, not as measurements.\n\n**Not available off-axis.** `:\u03c3x`, `:\u03c3y`, `:\u03c3z`, `:\u03c3`, `:\u03c3r_cylinder`, `:\u03c3\u03d5_cylinder`, `:r_cylinder`, `:r_sphere`, `:\u03d5` are tied to the box axes and are rejected with a clear error. **The exit ramp:** if you want any of those, use the axis-aligned path (`direction=:x/:y/:z`) \u2014 which is also faster and populates `maps_lmax`. If you want line-of-sight kinematics along +z specifically, that is still this page: write `los=[0,0,1]` (Chapter 7)."
      },
      {
       "kind": "code",
       "content": "mv = projection(gas, [:mass, :T], [:Msol, :K];\n                inclination=35, axis=:angmom, center=[:bc],\n                fov=20, fov_unit=:kpc, aperture=:square, pxsize=[0.3,:kpc],\n                weighting=[:mass, missing], verbose=false, show_progress=false)\n\n# a mass-weighted call ships a free :sd map you did not ask for \u2014 expect it when you iterate keys\nprintln(\"maps returned : \", collect(keys(mv.maps)))\nprintln(\"units         : \", mv.maps_unit)\n\n# \u2500\u2500 the conservation guarantee, measured once, on the WHOLE box (no fov, no window) \u2500\u2500\nmtot = projection(gas, :mass, :Msol; los=[1,1,1], center=[:bc],\n                  pxsize=[0.5,:kpc], verbose=false, show_progress=false)\nprintln(\"\u03a3(map) / msum(gas) \u2212 1  =  \", sum(mtot.maps[:mass]) / msum(gas, :Msol) - 1)"
      },
      {
       "kind": "markdown",
       "content": "That relative error is at the floating-point floor, and it stays there at **any** viewing angle, **any** pixel size and **any** `binning` \u2014 the deposit is a partition of unity, so every cell's weight is fully accounted for somewhere on the map. Appendix B says where the systematic sweep that establishes this lives.\n\nOne consequence worth knowing: cells whose deposit stencil crosses the map edge fold the outside fraction onto the **edge pixel**. Conservation is preserved by piling that mass into the outermost row, so the last radial bin of any profile taken from an off-axis map is wrong. Take profiles from a frame larger than the region you care about."
      }
     ]
    },
    {
     "n": 6,
     "title": "Placement: which binning",
     "objective": "Reader can say what binning does and does not change, can tell which resolution regime they are in, and can choose a kernel deliberately.",
     "cells": [
      {
       "kind": "markdown",
       "content": "Chapter 5 settled the total. This chapter is about the **other** question: *where* the mass lands.\n\nA rotated cube's shadow on the image plane is a hexagon that generally straddles several pixels. The four `binning` kernels are four answers to \"how is that shadow shared out\" \u2014 they all share it out completely (hence one total), but they place it differently.\n\n![Four kernels, one footprint](assets/offaxis/offaxis_cell_treatment_v2.svg)\n\n| `binning` | what it does | use it for |\n|---|---|---|\n| `:ngp` | all weight into the pixel containing the cell centre | fastest preview; holes and moir\u00e9 when pixels are finer than cells |\n| `:cic` | bilinear split over the 2\u00d72 neighbouring pixels | fast preview; smoother, still no footprint |\n| `:overlap` **(default)** | the cube is supersampled over its true footprint \u2014 `n\u00b3` sub-points with `n = \u2308cellsize/pixel\u2309`, capped at `nmax=64`; cells past the cap deposit a footprint-sized top-hat, which is what keeps coarse cells hole-free | **everything you publish** |\n| `:exact` | the analytic footprint integral (a box-spline chord field over the hexagon) | the reference the others are checked against; no cap, slower |\n\n`:overlap` and `:exact` are threaded; `:ngp` and `:cic` run serially. `:exact` follows from the box-spline representation of a projected cube (de Boor, *Box Splines*); nothing about choosing a `binning` depends on that derivation, so it is not reproduced here.",
       "figure": "FIG 5 (schematic, REDRAW of the existing offaxis_cell_treatment.svg). Compress the current two-figure asset to ONE row of five 6\u00d76 pixel grids sharing an identical hexagonal cube shadow: the shadow \u2192 :ngp (one filled pixel) \u2192 :cic (2\u00d72, opacity = weight) \u2192 :overlap (sub-point lattice filling the hexagon) \u2192 :exact (hexagon filled with the chord-length gradient, pixel edges drawn on top). Keep the existing asset's best device: one plain-language line per panel with a grey `technical:` line beneath. Teaches that all four act on the SAME footprint and differ only in how it is shared \u2014 which four bullet points cannot. Cut: the current asset's 'a grid of cubes' / 'tilt it' panels, and the whole of offaxis_exact_integration.svg."
      },
      {
       "kind": "code",
       "content": "# Which regime are you in? Binning only matters when PIXELS ARE FINER THAN CELLS.\nfor l in sort(unique(getvar(gas, :level)))\n    cs = info.boxlen / 2^l\n    println(\"level \", l, \":  cell \", rpad(round(cs, digits=2), 5), \" kpc\",\n            \"  \u2192  \", round(cs / 0.1, digits=1), \" pixels per cell at pxsize = 0.1 kpc\")\nend\n\nzoom = (center=[:bc], fov=8, fov_unit=:kpc, aperture=:square,\n        pxsize=[0.1,:kpc], verbose=false, show_progress=false)\np_cic = projection(gas, :sd, :Msol_pc2; inclination=60, axis=:angmom, binning=:cic,     zoom...)\np_ovl = projection(gas, :sd, :Msol_pc2; inclination=60, axis=:angmom, binning=:overlap, zoom...)\n\nfor (nm, p) in ((\"cic\", p_cic), (\"overlap\", p_ovl))\n    A = p.maps[:sd]\n    println(rpad(nm, 9), \"empty pixels: \", rpad(round(100*count(iszero, A)/length(A), digits=1), 5),\n            \" %    \u03a3 = \", round(sum(A), sigdigits=10))\nend"
      },
      {
       "kind": "code",
       "content": "# \u2500\u2500 figure code from here: panels, overlays, colorbars \u2014 no new Mera concepts \u2500\u2500\ncr = sharedrange([p_cic, p_ovl], :sd)\nmaprow([p_cic, p_ovl], :sd,\n       [\"binning=:cic  (preview)\", \"binning=:overlap  (default)\"]; crange=cr)",
       "figure": "FIG 6. Two panels, one shared log colour scale, interpolate=false, rendered at \u22651 screen pixel per map pixel \u2014 the speckle IS the subject and must not be smoothed away. Zoomed to an 8 kpc window at 0.1 kpc pixels so cells span 8\u201331 pixels: :cic shows holes and a dotted texture, :overlap fills the footprints. The printed totals immediately above are identical to ten significant digits. Together they carry the chapter's whole claim: same total, different placement."
      },
      {
       "kind": "markdown",
       "content": "**So:** leave `binning` alone. The default is the accurate one. Reach for `:ngp`/`:cic` only when you are iterating on a preview and can see that your pixels are coarser than your cells; reach for `:exact` only when you want the analytic reference."
      }
     ]
    },
    {
     "n": 7,
     "title": "Line-of-sight kinematics",
     "objective": "Reader can map :vlos and :\u03c3los for any camera, knows the sign convention, and knows that \u03c3_LOS is a property of the map rather than of a cell.",
     "cells": [
      {
       "kind": "markdown",
       "content": "`:vlos` is `v\u00b7\u0175`, the component of the velocity along the line of sight \u2014 defined for **any** camera. That is what makes it different from `:\u03c3x`/`:\u03c3y`/`:\u03c3z`, which only exist along the box axes and are rejected off-axis.\n\n`:\u03c3los` is `\u221a(\u27e8v_LOS\u00b2\u27e9 \u2212 \u27e8v_LOS\u27e9\u00b2)` over the mass in a pixel. It is a **width of a distribution inside one pixel**, not a per-cell quantity \u2014 many cells along the ray land in the same pixel, each with its own `v\u00b7\u0175`, and \u03c3 is how spread out they are. Two things follow immediately, and the code cells below measure both:\n\n- it depends on `pxsize` (bigger pixels beam-smear the velocity gradient into extra \u03c3), and\n- edge-on, it is dominated by *ordered rotation along the sightline*, not by turbulence. Do not call it a turbulent dispersion."
      },
      {
       "kind": "markdown",
       "content": "!!! warning \"Sign convention\"\n    `\u0175` points **into** the image, away from the observer, so `v\u00b7\u0175 > 0` is **receding** (redshifted). A sign flip inverts a rotation curve and nothing else in the figure changes, so check it: on an edge-on map, `:vlos` must be **antisymmetric about the minor axis**. If it is not, your `center` is off the object (Chapter 1)."
      },
      {
       "kind": "code",
       "content": "kin = (center=[:bc], fov=15, fov_unit=:kpc, aperture=:square,\n       pxsize=[0.15,:kpc], verbose=false, show_progress=false)\n\nkeo = projection(gas, [:vlos, :\u03c3los], [:km_s, :km_s]; direction=:edgeon, kin...)\nkfo = projection(gas, [:vlos, :\u03c3los], [:km_s, :km_s]; direction=:faceon, kin...)\n\nfinite(A) = filter(isfinite, vec(Float64.(A)))\nprintln(\"edge-on   max |v_LOS| = \", round(maximum(abs, finite(keo.maps[:vlos])), digits=1), \" km/s\")\nprintln(\"face-on   max |v_LOS| = \", round(maximum(abs, finite(kfo.maps[:vlos])), digits=1), \" km/s\")"
      },
      {
       "kind": "code",
       "content": "# \u2500\u2500 figure code from here: panels, overlays, colorbars \u2014 no new Mera concepts \u2500\u2500\nvmax  = maximum(abs, finite(keo.maps[:vlos]))\nsr    = sharedrange([keo, kfo], :\u03c3los; logscale=false)\n\nfig = Figure(size=(1100, 400))\nax1 = Axis(fig[1,1], title=\"edge-on  v_LOS\", xlabel=\"x' [kpc]\", ylabel=\"y' [kpc]\")\nh1  = showmap!(ax1, keo, :vlos; logscale=false, cmap=:balance, crange=(-vmax, vmax))\nColorbar(fig[1,2], h1, label=\"v_LOS [km/s]\")\nax2 = Axis(fig[1,3], title=\"edge-on  \u03c3_LOS\", xlabel=\"x' [kpc]\")\nh2  = showmap!(ax2, keo, :\u03c3los; logscale=false, cmap=:viridis, crange=sr)\nax3 = Axis(fig[1,4], title=\"face-on  \u03c3_LOS\", xlabel=\"x' [kpc]\")\nshowmap!(ax3, kfo, :\u03c3los; logscale=false, cmap=:viridis, crange=sr)\nhideydecorations!(ax2, grid=false); hideydecorations!(ax3, grid=false)\nColorbar(fig[1,5], h2, label=\"\u03c3_LOS [km/s]\")\nfig",
       "figure": "FIG 7. Three panels. Panel 1: edge-on v_LOS on a diverging map with symmetric limits \u00b1max|v| \u2014 the rotation butterfly, and the visible antisymmetry the caveat told the reader to check. Panels 2 and 3: \u03c3_LOS edge-on and face-on on ONE shared sequential range \u2014 edge-on \u03c3 is the projected rotation width, face-on \u03c3 is the vertical dispersion of the same gas. Same quantity, same data, completely different map, and the only thing that changed is \u0175. That is the entire argument for :vlos/:\u03c3los existing alongside the axis-tied :\u03c3x/:\u03c3y/:\u03c3z. No face-on v_LOS panel: with the shared symmetric range it renders as a blank rectangle, so the printed maximum above carries that fact instead."
      },
      {
       "kind": "code",
       "content": "# \u03c3_LOS is a width measured INSIDE a pixel, so it depends on the pixel.\nkcoarse = projection(gas, :\u03c3los, :km_s; direction=:edgeon, center=[:bc],\n                     fov=15, fov_unit=:kpc, aperture=:square, pxsize=[0.6,:kpc],\n                     verbose=false, show_progress=false)\n\nmed(A) = median(filter(x -> isfinite(x) && x > 0, vec(Float64.(A))))\nprintln(\"median \u03c3_LOS @ 0.15 kpc pixels : \", round(med(keo.maps[:\u03c3los]),     digits=1), \" km/s\")\nprintln(\"median \u03c3_LOS @ 0.60 kpc pixels : \", round(med(kcoarse.maps[:\u03c3los]), digits=1), \" km/s\")"
      },
      {
       "kind": "markdown",
       "content": "Those two numbers are the same physics at two pixel sizes. Always quote `pxsize` next to a \u03c3_LOS number.\n\n!!! note \"Shipped separately\"\n    Position\u2013position\u2013velocity cubes, emission and absorption forward modelling, mock observations and FITS export are **in development in a separate module** and are not part of the released package. This page covers only the moment maps `:vlos` and `:\u03c3los`. (Stated once, here.)"
      }
     ]
    },
    {
     "n": 8,
     "title": "Cutting planes: a sample, not an integral",
     "objective": "Reader can produce a plane cut, and can say why it is not a projection and must not be summed.",
     "cells": [
      {
       "kind": "markdown",
       "content": "`slice` is the cutting-plane function. It takes the same view keywords as `projection` and returns the cells the plane passes through. (`offaxis_slice` is a documented alias of the same function; prefer `slice`.)\n\nIt is a **nearest-cell sample**, not an integral. Three consequences:\n\n- **It is not mass-conserving.** There is nothing to conserve \u2014 no ray is integrated. Never sum a slice.\n- **Empty (NaN) pixels are geometry, not failure.** Where the plane threads between refinement levels there is simply no cell.\n- **The tilted, elongated blocks are the true shape of the cut** \u2014 a plane crossing an axis-aligned cube at an angle gives a quadrilateral cross-section, and that is what you see.\n\nAnd one API trap that is the *opposite* of Chapter 4: on `slice`, `xrange`/`yrange` **are the camera-plane window**. There is no `fov`, no `zrange`, no `binning`; it takes exactly one variable and works on cell data only (hydro, gravity, RT \u2014 `slice(part, \u2026)` is a `MethodError`). Its `.extent` is code units and its `.center` really is code units too, unlike a map's."
      },
      {
       "kind": "code",
       "content": "view = (inclination=60, azimuth=30, axis=:angmom, center=[:bc])\n\n# slice: xrange/yrange ARE the camera window here\nsl = slice(gas, :rho, :nH; view..., xrange=[-15,15], yrange=[-15,15],\n           range_unit=:kpc, pxsize=[0.12,:kpc], verbose=false)\n\n# projection: same view, and `fov` is how you get the SAME \u00b115 kpc camera frame\npr = projection(gas, :rho, :nH; view..., fov=15, fov_unit=:kpc, aperture=:square,\n                pxsize=[0.12,:kpc], verbose=false, show_progress=false)\n\nprintln(\"slice frame      \", size(sl.map), \"   \", round(100*count(isnan, sl.map)/length(sl.map), digits=1), \" % NaN\")\nprintln(\"projection frame \", size(pr.maps[:rho]))\nprintln(\"slice extent [kpc] = \", round.(sl.extent .* gas.scale.kpc, digits=1))"
      },
      {
       "kind": "code",
       "content": "# \u2500\u2500 figure code from here: panels, overlays, colorbars \u2014 no new Mera concepts \u2500\u2500\nAs = log10.(replace(Float64.(sl.map), 0.0 => NaN))\nAp = log10.(replace(Float64.(pr.maps[:rho]), 0.0 => NaN))\ncr = (quantile(filter(isfinite, vcat(vec(As), vec(Ap))), 0.02),\n      maximum(filter(isfinite, vcat(vec(As), vec(Ap)))))\nes = sl.extent .* gas.scale.kpc\n\nfig = Figure(size=(880, 400))\nax1 = Axis(fig[1,1], aspect=DataAspect(), title=\"projection \u2014 column integral\",\n           xlabel=\"x' [kpc]\", ylabel=\"y' [kpc]\")\nh = showmap!(ax1, pr, :rho; crange=cr, cmap=:viridis)\nax2 = Axis(fig[1,2], aspect=DataAspect(), title=\"slice \u2014 nearest-cell sample\", xlabel=\"x' [kpc]\")\nheatmap!(ax2, range(es[1],es[2],length=size(As,1)), range(es[3],es[4],length=size(As,2)),\n         As, colormap=:viridis, colorrange=cr, nan_color=:black, interpolate=false)\nhideydecorations!(ax2, grid=false)\nColorbar(fig[1,3], h, label=\"log10 n_H [cm\u207b\u00b3]\")\nfig",
       "figure": "FIG 8. Two panels, identical view, identical pxsize, ONE shared log colour scale and colourbar. Left: the smooth column. Right: individual tilted cell polygons and black NaN specks (nan_color named in the caption so 'empty' and 'low' are not confused). The contrast is the content \u2014 a gallery of three slices at three angles teaches only that slices exist."
      }
     ]
    },
    {
     "n": 9,
     "title": "Turning the camera: orbit movies",
     "objective": "Reader can render a jitter-free angle sweep and knows why its framing rule differs from a single projection.",
     "cells": [
      {
       "kind": "markdown",
       "content": "`rotation_sequence` renders one variable from a list of angles and returns a `Vector` of map objects. `sweep=:azimuth` (default), `:inclination` or `:position_angle` chooses which angle varies.\n\nIt uses **the same framing rule as Chapter 4, swept**: a rotation-invariant sphere selection, `aperture=:circle` or `:square`. That is the whole reason it exists \u2014 a cubic window's rotated bounding box changes size frame by frame, so the movie breathes.\n\n!!! note \"One difference from `projection`\"\n    Omit `fov` on `rotation_sequence` and it **auto-fits** to the 99 % enclosed-mass radius, so the frame follows the object rather than the sparse outermost cells. `projection` has **no** auto-fit \u2014 omit `fov` there and you are back on world ranges. If you learned `fov=22` in Chapter 4 and drop it here, your framing rule silently changes. Pass `fov` explicitly whenever it matters.\n\n`parallel_frames=true` runs the frames concurrently with single-threaded projections instead of the reverse; it is typically ~1.5\u20132\u00d7 faster when there are at least as many frames as threads, at proportionally more transient memory. Keep the total at or below 8 threads on a laptop."
      },
      {
       "kind": "code",
       "content": "frames = rotation_sequence(gas, :sd, :Msol_pc2; sweep=:azimuth, angles=0:90:270,\n                           inclination=55, axis=:angmom, center=[:bc],\n                           fov=22, fov_unit=:kpc, aperture=:square,\n                           pxsize=[0.5,:kpc], parallel_frames=false)\n\n# The numerical proof that the frame does not breathe \u2014 stronger than watching it.\nfor (a, f) in zip(0:90:270, frames)\n    println(\"azimuth \", lpad(a,3), \"\u00b0   frame \", size(f.maps[:sd]),\n            \"   extent [kpc] = \", round.(getextent(f, :kpc), digits=3))\nend"
      },
      {
       "kind": "markdown",
       "content": "Every frame is the same array, the same physical extent and the same pixel scale. To turn that into a movie, animate with a **fixed colour range across all frames** (otherwise the animation flickers and destroys the very stability it is demonstrating), then encode:\n\n```julia\n# not executed in the docs build \u2014 ffmpeg is not a documentation dependency\ncr = sharedrange(frames, :sd)\nfig = Figure(size=(500,500)); ax = Axis(fig[1,1])\nrecord(fig, \"orbit.mp4\", eachindex(frames); framerate=12) do k\n    empty!(ax); showmap!(ax, frames[k], :sd; crange=cr)\nend\n```\n\n![Orbit movie](assets/offaxis/orbit_movie.mp4)",
       "figure": "FIG 9 (REUSE the existing pre-rendered assets/offaxis/orbit_movie.mp4, with orbit_movie.gif as fallback). The only figure in the notebook whose subject is motion: a static montage cannot show that the frame does not breathe. Pre-rendered deliberately \u2014 live ffmpeg encoding is a fragile documentation-build dependency, and the printed frame sizes above already give the numerical proof."
      }
     ]
    },
    {
     "n": 10,
     "title": "Reading the result, and fixing a map that looks wrong",
     "objective": "Reader can diagnose an off-axis image that looks wrong, and can persist a map instead of re-projecting it.",
     "cells": [
      {
       "kind": "markdown",
       "content": "Everything you need to debug an off-axis map travels on the result. You already printed the camera in Chapter 1; here is how to read it.\n\n- `m.los`, `m.up`, `m.cam_right` \u2014 the basis actually used. If `m.los` is not what you expected, your view specifier or your `center` is the problem, not the renderer.\n- `m.center` is a **box fraction** on a map object (a `slice` result's `.center` is code units \u2014 different quantity, same field name).\n- `m.direction` reads `:offaxis`.\n- `m.maps_lmax` is **empty** off-axis by design; `m.maps_weight` exists on AMR map objects only, not on particle ones.\n- `m.pixsize` and `m.extent` are code units \u2014 use `getextent(m, :kpc)`.\n\n**Orientation fixes.** `position_angle=` rolls the image about `\u0175`; `up=` pins the image-y direction outright. Note that when you write your own loop over hand-made `los=` vectors, the automatic up-vector (the world axis least parallel to `\u0175`) can **flip mid-sweep** and your animation will jump. Fix `up=` explicitly, or use `rotation_sequence`, which sets an up-hint for you.\n\n**Grid diagnostics.** `gridoverlay(gas; level=:max, <same view kwargs>)` draws the AMR structure in the camera frame. Read it honestly: off-axis it draws **cell-size squares at rotated cell centres**, not the true projected hexagonal footprints, so it tells you *where* and *how big* cells are, not what shape their shadows have."
      },
      {
       "kind": "code",
       "content": "savemap(fo, \"faceon_sd.jld2\", verbose=false)\nfo2 = loadmap(\"faceon_sd.jld2\", verbose=false)\n\nprintln(\"camera survives the round trip : \", fo2.los == fo.los && fo2.up == fo.up)\nprintln(\".center (box fraction)         : \", round.(fo.center, digits=3))\nprintln(\".maps_lmax off-axis            : \", isempty(fo.maps_lmax) ? \"empty (by design)\" : \"populated\")\nprintln(\".direction                     : \", fo.direction)"
      },
      {
       "kind": "markdown",
       "content": "**Troubleshooting** \u2014 six symptoms, six causes:\n\n| symptom | cause | fix |\n|---|---|---|\n| `:edgeon` looks face-on, or the disc is tilted at a random angle | L computed about the box corner | pass `center=[:bc]` (Ch 1) |\n| \"face-on\" is off by 10\u201330\u00b0 for a warped or fountain-rich disc | L over the full box is dominated by slow, high-lever-arm CGM gas | take `:angmom` on an inner-disc `subregion`, then project the full data with that explicit `los=` (App. C) |\n| straight edges cut across the map; the frame is bigger than requested | world-space `xrange`/`yrange` | `fov` + `fov_unit` + `aperture=:square` (Ch 4) |\n| the galaxy is a dot in a huge frame | `fov` without `fov_unit` \u2014 22 *box lengths*, clamped | always pair them (Ch 4) |\n| `projection(gas, :vlos; direction=:z)` errors in `getvar` | `:vlos`/`:\u03c3los` exist only in the off-axis engine | write `los=[0,0,1]` (Ch 5) |\n| holes or moir\u00e9 in a zoomed map | `:cic`/`:ngp` with pixels finer than cells | `binning=:overlap` (Ch 6) |\n| an orbit movie tumbles across snapshots | `direction=:faceon` recomputed per snapshot; L drifts | freeze one `los=` (App. C) |\n\nAnd when re-plotting is the slow part, do not re-project: `savemap` / `loadmap` round-trip the whole object \u2014 every map and unit, the geometry, the camera basis and the simulation `info`."
      }
     ]
    },
    {
     "n": 11,
     "title": "The same camera on other data",
     "objective": "Reader knows the call is identical for particles and gravity, and knows the handful of ways their keywords differ.",
     "cells": [
      {
       "kind": "markdown",
       "content": "The view keywords are the same everywhere. What differs is a short list of defaults and gaps, all in one table:\n\n| | hydro / RT | particles |\n|---|---|---|\n| `binning` default | `:overlap` | **`:cic`**; `:overlap`/`:exact` silently fall back to `:cic` (points have no footprint) |\n| `weighting` | **Array**, `[:mass, missing]` | **Symbol**, `:mass` \u2014 and `:sph`/`:voronoi` are accepted but silently give a mass-weighted map off-axis |\n| `fov` / `fov_unit` / `aperture` | yes | **absent** \u2014 passing them is a `MethodError`; use world ranges with an explicit `zrange` |\n| `mode`, `nmax`, `max_threads`, `gravity_data` | yes | absent |\n| `data_center` | **silently ignored** on the off-axis hydro path | honoured |\n| `slice` | yes | no \u2014 `slice(part, \u2026)` is a `MethodError` |\n\nGravity goes through the combined call form `projection(hydro, gravity, var; \u2026)`. RT data has no velocity field, so `:angmom`, `:faceon` and `:edgeon` are unavailable \u2014 give an explicit `los=`, or take `[:lx,:ly,:lz]` from the matching hydro object with `getvar` and pass the result as `axis=[lx,ly,lz]`. (This fixture has no RT output; see the [radiative transfer page](10_multi_RadiativeTransfer.md).)"
      },
      {
       "kind": "code",
       "content": "part = getparticles(info, verbose=false, show_progress=false)\nps = projection(part, :sd, :Msol_pc2; direction=:edgeon, center=[:bc],\n                xrange=[-20,20], yrange=[-20,20], zrange=[-20,20], range_unit=:kpc,\n                pxsize=[0.4,:kpc], verbose=false, show_progress=false)\nprintln(\"stars   : frame \", size(ps.maps[:sd]), \"   los = \", round.(ps.los, digits=3))\n\ngrav = getgravity(info, verbose=false, show_progress=false)\npe = projection(gas, grav, :epot; direction=:faceon, center=[:bc],\n                xrange=[-20,20], yrange=[-20,20], zrange=[-20,20], range_unit=:kpc,\n                pxsize=[0.4,:kpc], verbose=false, show_progress=false)\nprintln(\"gravity : maps \", collect(keys(pe.maps)), \"   epot extrema \",\n        round.(extrema(pe.maps[:epot]), sigdigits=3))"
      },
      {
       "kind": "markdown",
       "content": "No figures here on purpose. An edge-on stellar surface density and a face-on potential look exactly as Chapters 1 and 3 already taught you they would; the content of this chapter is the keyword table above, not two more pictures of the same galaxy."
      }
     ]
    },
    {
     "n": 12,
     "title": "Appendix A \u2014 Limitations, in one place",
     "objective": "Reader can check any assumption they are about to make against a single list.",
     "cells": [
      {
       "kind": "markdown",
       "content": "The one limitations table. Twelve rows, one line each. Nothing here is repeated anywhere else in the notebook.\n\n| | |\n|---|---|\n| **Orthographic only** | parallel rays, observer at infinity. No perspective, no observer-inside-the-box view. |\n| **No line-of-sight depth slab** | `xrange`/`yrange`/`zrange` are world-space; the frame always auto-fits the rotated footprint. `zrange` clips world *z*, and only coincides with depth near a \u00b1z line of sight. |\n| **`zrange` can be a no-op** | an axis whose requested range already covers the loaded data's range is not clipped at all. |\n| **Slab edges are cell-centre cuts** | cells are kept or dropped by their centre; straddlers are not clipped, so a world-range selection is accurate to about one cell size per face (3.1 kpc at level 5 here). |\n| **Border fold-back** | stencils crossing the map edge fold onto the edge pixel \u2014 the outermost row/column of any off-axis map is not a clean measurement. |\n| **`fov` footguns** | `fov_unit` defaults to a box fraction; `fov` replaces `xrange`/`yrange`/`zrange`; `center` is then read in `fov_unit`; `fov` cannot be combined with a per-cell `mask`. |\n| **Frames match, but not bit-exactly** | `aperture=:square` gives identical pixel dimensions and identical physical scale at every angle; co-registration is good to the auto-fit rounding, not to the bit. |\n| **`nmax=64`** | zoom far enough into a coarse-AMR region and `:overlap` deposits a footprint-sized top-hat rather than a 64\u00b3 sub-point lattice. Detect by raising `nmax`, or use `:exact` (no cap). |\n| **Off-axis \u2260 axis-aligned pixel grids** | differencing `los=[0,0,1]` against `direction=:z` shows a sub-pixel origin offset. That is a grid convention, not an error. |\n| **Variables unavailable off-axis** | `:\u03c3x :\u03c3y :\u03c3z :\u03c3 :\u03c3r_cylinder :\u03c3\u03d5_cylinder :r_cylinder :r_sphere :\u03d5` \u2014 use `direction=:x/:y/:z`. |\n| **Result-object quirks** | `maps_lmax` is empty off-axis; `maps_weight` is AMR-only; `.center` is a box fraction on a map but code units on a `slice`; a mass-weighted call returns a bonus `:sd` map; `lmax` off-axis only sets the default resolution and never coarsens the data. |\n| **Shipped separately** | column integrals, emission/absorption, PPV cubes, mock observations and FITS export are in development in a separate module, not in the released package. |"
      }
     ]
    },
    {
     "n": 13,
     "title": "Appendix B \u2014 Where the guarantees are measured",
     "objective": "Reader can find the systematic evidence behind the single number printed in Chapter 5.",
     "cells": [
      {
       "kind": "markdown",
       "content": "Chapter 5 measured conservation once. The question this appendix answers is *\"how do I know it holds beyond that one case?\"* \u2014 six lines, no figures, no re-runs.\n\n- **Angle \u00d7 pixel size \u00d7 binning sweep.** `test/<FILL IN test file>` projects `:mass` over 7 viewing angles \u00d7 5 pixel sizes \u00d7 3 binnings and asserts the relative error against `msum`. Worst observed |relerr| \u2248 1\u00d710\u207b\u00b9\u2075 \u2014 the floating-point floor, not a tolerance.\n- **`:overlap` against the analytic reference.** The mean per-pixel L1 difference between `:overlap` and `:exact` on the same view is \u2248 0.04 in units of the mean pixel value: the default is a supersampled approximation of the analytic footprint integral, and it is a close one.\n- **Kinematics oracle.** `test/<FILL IN>` checks `:vlos` against an analytically known solid-body rotation field \u2014 this is the test that catches a sign flip, which no conservation check can.\n- **Threading, at the 8-thread laptop ceiling.** The `:overlap`/`:exact` deposits thread over cells; `:ngp`/`:cic` are serial. Measured on this fixture at `pxsize=[0.3,:kpc]`: **<FILL IN: 1 / 4 / 8 threads, three numbers>**. Scaling is sub-linear \u2014 the per-cell geometry setup and the final reduction are serial (Amdahl), and the deposit is memory-bound.\n- **Reference for `:exact`.** C. de Boor, K. H\u00f6llig, S. Riemenschneider, *Box Splines*, Springer (1993) \u2014 the projected image of a cube is a box spline, which is what makes an exact per-pixel footprint integral tractable."
      }
     ]
    },
    {
     "n": 14,
     "title": "Appendix C \u2014 Recipe: one fixed orientation across a time series",
     "objective": "Reader can keep a movie or a measurement series from tumbling as the disc's angular momentum drifts.",
     "cells": [
      {
       "kind": "markdown",
       "content": "The most common way a multi-snapshot off-axis analysis goes wrong: `direction=:faceon` is called per snapshot, the disc's L drifts (warp, bar, merger, an infalling satellite), and the resulting movie tumbles or the measured inclination wanders.\n\nThe fix is to compute the orientation **once**, on the region you actually mean, and then freeze it:\n\n```julia\n# NOT EXECUTED \u2014 this fixture ships a single output (output_00100).\n# Shown for the mechanics; substitute your own output list.\n\n# 1. Orientation from the INNER DISC of one reference snapshot, not the whole box.\n#    L over the full box is dominated by slow, high-lever-arm CGM gas, which is\n#    why a whole-box \"face-on\" can be 10\u201330\u00b0 off for a warped or fountain-rich disc.\nref  = gethydro(getinfo(100, path, verbose=false), verbose=false, show_progress=false)\ndisc = subregion(ref, :cylinder, radius=8., height=2., range_unit=:kpc,\n                 center=[:bc], verbose=false)\nLfix = [sum(getvar(disc, :lx, center=[:bc])),\n        sum(getvar(disc, :ly, center=[:bc])),\n        sum(getvar(disc, :lz, center=[:bc]))]\nLfix ./= sqrt(sum(abs2, Lfix))\n\n# 2. Project every snapshot with that ONE frozen line of sight and a fixed FOV.\n#    `up=` is pinned too, so the auto-up tie-break cannot flip mid-series.\nfor out in outputs\n    g = gethydro(getinfo(out, path, verbose=false), verbose=false, show_progress=false)\n    m = projection(g, :sd, :Msol_pc2; los=Lfix, up=[0,0,1], center=[:bc],\n                   fov=22, fov_unit=:kpc, aperture=:square, pxsize=[0.3,:kpc],\n                   verbose=false, show_progress=false)\n    savemap(m, \"faceon_$(out).jld2\", verbose=false)\nend\n```\n\nGas L and stellar L are not the same vector (bars, warps, counter-rotating components), so decide which tracer defines your frame and stick to it. And note that this is also how you get a *comparable* frame: the same `los`, the same `fov`, the same `pxsize`, so every snapshot is the same camera."
      }
     ]
    }
   ],
   "cut_list": [
    {
     "what": "The `los=[0,0,1]` opening projection and its map (guide L200\u2013224)",
     "why": "A picture deliberately chosen to be indistinguishable from an axis-aligned map is a test, not a lesson \u2014 and it is the reader's FIRST image. The identity claim survives as one clause in Ch 2."
    },
    {
     "what": "Section 'How the camera basis is built (the math)' \u2014 Gram\u2013Schmidt, auto-up tie-break, roll matrices, R(p\u2212c) (guide L126\u2013198)",
     "why": "Formalism 150 lines before the first tilted image. Ch 2 keeps the four lines a user needs; the construction goes to `?projection` and api/offaxis.md. NOT relocated to an appendix \u2014 that is exactly how the current pages reached 1734 lines."
    },
    {
     "what": "The static azimuth row of offaxis_views.png, and the standalone `los=[1,1,1], position_angle=30` map",
     "why": "Azimuth has no observational counterpart; four azimuths of a clumpy disc at fixed inclination on a shared colour scale render as four near-identical smears, so the figure would actively teach that azimuth does nothing. Azimuth lives in the schematic (an arc) and the movie (motion); position_angle reappears in Ch 10 as a fix, which is what it is for."
    },
    {
     "what": "Section 'Accuracy of off-axis projection' (guide L550\u2013586)",
     "why": "Five bullets restating the binning section and the conservation page. Its two non-redundant items (slab edges, no intermediate resampling) become two rows of the single limitations table."
    },
    {
     "what": "Section 'Field of view and depth' as written",
     "why": "It states that xrange/yrange define a camera-plane window and that zrange acts as a line-of-sight depth slab. Both are false \u2014 clipping is on world px/py/pz, and z_cam is computed once at projection_hydro.jl:1667 and never referenced again. Ch 4 replaces it with the verified behaviour."
    },
    {
     "what": "The live thread-scaling loop and the res=1024 conservation benchmark",
     "why": "Machine-dependent timing loops that run on every docs build, teach nothing about off-axis projection, and blow the runtime and 8-thread budgets. Three printed numbers plus one Amdahl sentence in App B."
    },
    {
     "what": "The box-spline derivation \u2014 zonotopes, kink lines, Sutherland\u2013Hodgman, \u22646 convex pieces",
     "why": "Nobody chooses a `binning` from it. Two sentences plus the de Boor citation."
    },
    {
     "what": "offaxis_internals.md \u00a7\u00a71, 2, 6, 9, 10, 11",
     "why": "Duplicates of Chs 3, 6, 8, 9. \u00a79 is the worst: it introduces 'slice is not mass-conserving' on a page that never establishes conservation."
    },
    {
     "what": "offaxis_conservation_proof.md \u00a72b triptych, \u00a73 thread scaling, \u00a7'Relation to other tools'",
     "why": "\u00a72b is one number; \u00a73 duplicates the guide's scaling section; the tools comparison appears three times and violates the house rule against attributing techniques to other tools. The reference list survives, in App B."
    },
    {
     "what": "The second fixture spiral_ugrid and its live gethydro",
     "why": "spiral_clumps at levels 5\u20137 gives 3.1/1.6/0.78 kpc cells; an 8 kpc window at pxsize=[0.1,:kpc] is 8\u201331 pixels per cell \u2014 deep in the regime where kernels differ, and the ratio can be PRINTED per level rather than asserted in a footnote. One gethydro per notebook (RAM discipline), one galaxy thread."
    },
    {
     "what": "The rt_stromgren dataset, the RT Np1 map and the RT code cell",
     "why": "spiral_clumps/output_00100 has amr/hydro/grav/part and no RT. A second getinfo+gethydro for four suppressed lines is zero pedagogical yield. RT gets three sentences in Ch 11 and a cross-link."
    },
    {
     "what": "The gravity :epot and stellar surface-density FIGURES (offaxis_gravity.png, offaxis_particles.png and their notebook twins)",
     "why": "A face-on potential and an edge-on stellar disc look exactly as Chs 1 and 3 already taught. Ch 11's content is the keyword-divergence table; the code cells stay for coverage but print instead of plot."
    },
    {
     "what": "The gridoverlay figure, and the proposal to overlay it on the slice panel as proof of the cell polygons",
     "why": "Off-axis, gridoverlay draws cell-size SQUARES at rotated cell centres (gridoverlay.jl:82\u2013110), not true projected footprints \u2014 it would visibly fail to line up with a tilted cut and the figure's stated proof would become a visible contradiction. It survives as two honest sentences in Ch 10."
    },
    {
     "what": "offaxis_conservation.png and offaxis_scaling.png",
     "why": "The first is two panels of flat lines at 1e-16 \u2014 a plot whose message is 'nothing happens'; the printed relerr is stronger evidence. The second is a 12-thread benchmark in a repo that caps at 8, plotting three facts."
    },
    {
     "what": "offaxis_exact_integration.svg",
     "why": "Redundant with the :exact panel of the kernel schematic, which already colours the footprint by chord length. :exact is the reference mode, not the default, and does not warrant two of nine figure slots."
    },
    {
     "what": "Seven pre-rendered PNGs that duplicate an adjacent code cell's output (offaxis_views, offaxis_multivar, offaxis_particles, offaxis_gravity, offaxis_slice, orbit_montage) plus orbit_fov and orbit_aperture",
     "why": "In a single notebook the notebook output IS the figure; showing both leaves the reader unable to tell which one the code made. orbit_fov/orbit_aperture are superseded by Fig 4, which shows the artefact they only imply."
    },
    {
     "what": "The 60-line getinfo dump at the top of the guide",
     "why": "verbose=false plus four printed lines: cells, boxlen, levels, threads."
    },
    {
     "what": "The `part === nothing ? nothing : gethydro(...)` ternary in the conservation page",
     "why": "`part` is a PartDataType and is never nothing \u2014 a dead branch left over from a script, not teaching."
    },
    {
     "what": "The three-panel slice gallery (face-on / edge-on / inclined) and the fourth slice picture in internals",
     "why": "Three slices side by side teach only that slices exist. The informative comparison \u2014 slice versus projection at the same view \u2014 was never shown anywhere and is now Fig 8."
    },
    {
     "what": "The claim that rotation_sequence, slice and off-axis projection all work on hydro, particles and RT alike",
     "why": "False for slice: it dispatches on Union{HydroDataType, GravDataType, RtDataType} (covering_grid.jl:173), so slice(part, \u2026) is a MethodError. Corrected as a row of the Ch 11 table."
    },
    {
     "what": "Every mention of hydro_data= as a projection / rotation_sequence keyword",
     "why": "It exists only on getvar; passed to projection it lands in kwargs and dies as an unsupported keyword. Ch 11 gives the real workaround: take [:lx,:ly,:lz] from a hydro object and pass axis=[\u2026] or los=[\u2026]."
    },
    {
     "what": "The statement that kinematics maps use CIC and therefore conserve mass",
     "why": "The deposit dispatches on whatever `binning` is set (:overlap by default), and :vlos/:\u03c3los are ratios of two deposits \u2014 conservation is not a property they have. The claim now attaches only to :mass/:sd, in Ch 5."
    },
    {
     "what": "Both remaining printings of the camera metadata (guide \u00a7basis, guide \u00a7metadata, internals \u00a75 \u2014 three in total)",
     "why": "Printed exactly once, in Ch 1, where it proves 'the orientation came from your data'. Ch 10 interprets those same fields and prints nothing new except the savemap round trip."
    }
   ],
   "figures": [
    {
     "name": "FIG 1 \u2014 Same data, different object (face-on | edge-on)",
     "type": "rendered",
     "teaches": "That one keyword turns a galaxy inside out with no reload and no re-gridding. The single most compelling fact about the feature, moved from 33 % into the current page to the first 5 %. No sentence conveys 'same 590 311 cells, only the camera moved'.",
     "reuse_existing": "new (live notebook output; retires assets/offaxis/offaxis_views.png)"
    },
    {
     "name": "FIG 2 \u2014 The camera: orthographic rays and the basis (r\u0302, \u00fb, \u0175)",
     "type": "schematic",
     "teaches": "That rays are parallel with no vanishing point (hence no camera distance and hence why `fov` must exist), and that inclination / azimuth / position_angle are three rotations about three different axes. Small inset: box-z and L drawn visibly non-parallel, because choosing the wrong reference axis is the most common silent error.",
     "reuse_existing": "docs/src/assets/offaxis/offaxis_geometry.svg \u2014 REDRAW: crop the keyword sidebar (it is a table, and Ch 3 has it), add the roll arc about \u0175, add the two-element :z-vs-:angmom inset. Save as offaxis_geometry_v2.svg."
    },
    {
     "name": "FIG 3 \u2014 Inclination ladder, i = 0 / 30 / 60 / 90\u00b0",
     "type": "rendered",
     "teaches": "The monotonic flattening of a clumpy spiral into a thin lane \u2014 a continuum prose cannot render. One shared colour scale, one colourbar, kpc axes on the left panel only. Deliberately NO azimuth row.",
     "reuse_existing": "new (live; panels 1 and 4 are the Ch 1 maps, so only two extra projections)"
    },
    {
     "name": "FIG 4 \u2014 What sets the frame: world ranges vs camera FOV",
     "type": "rendered",
     "teaches": "THE highest-value new figure. At fixed i=60\u00b0: (a) a requested \u00b122 kpc world box drawn as a dashed overlay inside a frame that is actually \u00b155 kpc, with the box's own faces visible as straight edges across the map; (b) fov=22, aperture=:square; (c) aperture=:circle. Array size printed in every panel title \u2014 column 1 changes size between angles, columns 2\u20133 do not. The current pages describe this artefact in prose only; a reader who has not seen it cannot recognise it in their own figure.",
     "reuse_existing": "new (retires assets/offaxis/orbit_fov.png and orbit_aperture.png)"
    },
    {
     "name": "FIG 5 \u2014 One cube, four kernels",
     "type": "schematic",
     "teaches": "That all four binnings act on the SAME footprint and differ only in how they share it out. Reading four bullet points, users conclude the kernels see different data. One row of five 6\u00d76 pixel grids: the shadow \u2192 :ngp \u2192 :cic \u2192 :overlap \u2192 :exact.",
     "reuse_existing": "docs/src/assets/offaxis/offaxis_cell_treatment.svg \u2014 REDRAW: compress the two welded figures to one row of five, drop the 'grid of cubes'/'tilt it' panels, absorb the :exact chord-length rendering so offaxis_exact_integration.svg can be deleted. Keep the existing per-panel plain-language + grey 'technical:' caption device. Save as offaxis_cell_treatment_v2.svg."
    },
    {
     "name": "FIG 6 \u2014 Same total, different placement (:cic vs :overlap, zoomed)",
     "type": "rendered",
     "teaches": "Holds 'visually different' and 'numerically identical to ten significant digits' in the eye at once. Rendered at \u22651 screen pixel per map pixel with interpolate=false \u2014 the speckle IS the subject. The totals are printed by the code cell immediately above rather than asserted in a caption.",
     "reuse_existing": "new, live on spiral_clumps (retires assets/offaxis/offaxis_fidelity.png and the second fixture it needed)"
    },
    {
     "name": "FIG 7 \u2014 Kinematics follow the camera (edge-on v_LOS | edge-on \u03c3_LOS | face-on \u03c3_LOS)",
     "type": "rendered",
     "teaches": "That the SAME quantity on the SAME data is a rotation butterfly or a smooth blob depending only on \u0175 \u2014 the entire argument for :vlos/:\u03c3los existing alongside the axis-tied :\u03c3x/:\u03c3y/:\u03c3z. Diverging map with symmetric shared limits for v_LOS; one shared sequential range across both \u03c3 panels. Also renders the antisymmetry the sign-convention caveat tells the reader to check.",
     "reuse_existing": "new (retires the guide's and internals' two separate kinematics figures; the \u03c3-in-a-pixel half of offaxis_vlos.png is replaced by a MEASUREMENT \u2014 median \u03c3_LOS at two pixel sizes \u2014 which is stronger than a schematic and also delivers the beam-smearing pitfall)"
    },
    {
     "name": "FIG 8 \u2014 Integral vs sample: projection | slice",
     "type": "rendered",
     "teaches": "That a slice is a nearest-cell sample, not a column \u2014 the tilted cell polygons and the black NaN specks are geometry, not artefacts. The CONTRAST is the lesson; three slices at three angles teach only that slices exist. One shared log colour scale and one colourbar; nan_color named in the caption so 'empty' and 'low' are distinguishable.",
     "reuse_existing": "new (retires assets/offaxis/offaxis_slice.png and the internals slice figure). Deliberately WITHOUT the gridoverlay panel \u2014 see cut list."
    },
    {
     "name": "FIG 9 \u2014 Orbit movie",
     "type": "rendered",
     "teaches": "The only figure whose subject is motion: that the frame does not breathe as the camera orbits. A static montage cannot show this. Fixed colour range across frames, burnt-in scale bar.",
     "reuse_existing": "docs/src/assets/offaxis/orbit_movie.mp4 (with orbit_movie.gif as fallback) \u2014 REUSE AS IS, pre-rendered. Live ffmpeg encoding is a fragile docs-build dependency; the notebook prints every frame's array size and extent instead, which is the numerical proof."
    }
   ],
   "open_questions": [
    "MEASURE FIRST, WRITE SECOND: the end-to-end wall clock at `julia -t 8` on spiral_clumps must be measured and put in Chapter 0 before any prose is written. It is the only number in this plan that cannot be argued with, and it decides whether the 20-projection budget survives. If it exceeds ~4 minutes, the first cuts are Ch 6's zoom resolution (0.1 \u2192 0.15 kpc) and Ch 9's frame count (4 \u2192 3).",
    "`projection(gas, grav, :epot; fov=\u2026)`: the `fov` branch subregions the HYDRO object via `_fov_selection` but forwards `gravity_data` unsubregioned, which looks like a cell-count mismatch waiting to happen. The Ch 11 code cell therefore uses world ranges for gravity, not `fov`. Please confirm whether this is a real bug (and file it) or whether the combined path handles it \u2014 it changes one code cell and possibly one limitations row.",
    "Chapter 1 prints `fo.cam_right`/`fo.up` for `direction=:faceon`. The current guide asserts that `los=[0,0,1], up=[0,1,0]` gives `r\u0302=[1,0,0], \u00fb=[0,1,0]`, but its own printed output shows `cam_right=[0,-1,0], up=[1,0,0]`. Decide which is correct before Ch 2's four-line basis description is written \u2014 if the printed values are right, the docstring text needs fixing too.",
    "Redirect policy for the two deleted pages. Documenter will not emit a 301; the options are (a) hard-delete both .md files and re-point the four inbound links, accepting that external bookmarks 404, or (b) leave two one-line stub pages that say 'merged into the off-axis page' with a link, kept out of the nav. Recommend (b) for one release, then (a). Your call.",
    "Appendix B has three <FILL IN> slots: the two test-file paths for the invariance sweep and the kinematics oracle, and the three thread timings. These must be filled from the actual test suite, not estimated.",
    "Whether Chapter 3's observer-vocabulary table should also state Mera's inclination sign convention (does i increase toward or away from the observer?). Nothing in the current pages says, and an observer matching a published (i, PA) will need it. If the convention is not currently documented anywhere, that is a source-level docstring gap as well.",
    "Notebook filename and location: this replaces /Volumes/FASTStorage/Simulations/Mera-Docs/version_1.1/06_offaxis_Projection.ipynb in place, or gets a new name? In-place keeps the generated .md path and every existing inbound link to 06_offaxis_Projection.md stable, which is the reason to prefer it \u2014 but it overwrites a working notebook, so commit the current one first.",
    "The two schematics to be redrawn (offaxis_geometry_v2.svg, offaxis_cell_treatment_v2.svg) are hand-made assets. Confirm who redraws them and in what tool, since the notebook cannot be built without them and they are the two figures the whole notebook hangs on."
   ],
   "page_plan": "This notebook REPLACES ALL THREE current pages. `offaxis_internals.md` and `offaxis_conservation_proof.md` are deleted outright, not folded into appendices.\n\nRationale. The three pages are organised around how Mera works (camera basis \u2192 kernels \u2192 conservation proof) rather than around what a reader does. The consequence is measurable: the binning story is told five times, conservation is demonstrated four times, the camera basis is derived twice, the multithreading benchmark appears twice, and roughly ten pre-rendered PNGs sit next to a code cell producing the identical picture. Consolidation, not redistribution, is the fix \u2014 and the single hardest discipline in this plan is that **nothing cut for redundancy may be relocated to an appendix; it is deleted.** That rule is what keeps the merged page from re-inflating, because the appendix landfill is precisely how 1734 lines accumulated.\n\nWhere the deleted material actually goes:\n* `offaxis_internals.md` \u00a7\u00a71, 2, 6, 9, 10, 11 \u2014 duplicates of the new Chs 3, 6, 8, 9. **Deleted.** \u00a72 (camera basis construction, Gram\u2013Schmidt, deterministic auto-up, roll matrices) is the one genuinely non-duplicated block: it moves into the `projection` docstring and `docs/src/api/offaxis.md`, where `?projection` already serves it. Chapter 2 keeps the four lines a user needs and says so explicitly. \u00a77 (extensive vs intensive) is promoted to Ch 5, where it belongs, because it is a correctness trap not an internals detail. \u00a713 (mass- vs volume-weighted T) moves to `06_hydro_Projection.md` \u2014 `weighting` is not off-axis-specific.\n* `offaxis_conservation_proof.md` \u2014 **deleted.** Its \u00a71 becomes one `@show` in Ch 5 (three lines). Its 7\u00d75\u00d73 invariance sweep, the `:overlap` vs `:exact` L1 number, and the kinematics oracle become five bullet lines in Appendix B pointing at the test files \u2014 a table of results, not a re-run. Its two figures are cut (see cut list). Its `spiral_ugrid` second fixture is dropped entirely.\n\nNav and links (someone must own this sweep):\n* `docs/make.jl:95\u201397` collapses the three-entry `\"Off-axis\" => Any[\u2026]` group to a single `\"Off-axis Projection\" => \"06_offaxis_Projection.md\"`.\n* Four inbound links must be re-pointed: `docs/src/api/offaxis.md:6` and `:58` (both to `../offaxis_internals.md`), and `docs/src/06_offaxis_Projection.md:22\u201323`, `:197`, `:494`, `:562` \u2014 the latter are internal to the page being rewritten, so they simply vanish. `offaxis_internals.md#2.-The-camera-basis` is an anchor link and must become an api-page anchor.\n* `docs/dev/REVAMP_PLAN.md:566` references `offaxis_internals.ipynb`; it is a private planning doc, so a note suffices.\n* Decide the redirect policy (see open questions).\n\nAssets: delete `offaxis_views.png`, `offaxis_multivar.png`, `offaxis_particles.png`, `offaxis_gravity.png`, `offaxis_slice.png`, `orbit_montage.png`, `orbit_fov.png`, `orbit_aperture.png`, `offaxis_fidelity.png`, `offaxis_conservation.png`, `offaxis_scaling.png`, `offaxis_exact_integration.svg`, `offaxis_vlos.png`, `offaxis_geometry.png` (the `.svg` is the source). Redraw two schematics from existing sources; keep `orbit_movie.mp4`/`.gif` untouched.\n\nSource of truth: the notebook at `/Volumes/FASTStorage/Simulations/Mera-Docs/version_1.1/06_offaxis_Projection.ipynb`, converted to `docs/src/06_offaxis_Projection.md`. Commit the current notebook before overwriting.",
   "thesis": "One notebook, one galaxy, one camera. The through-line is a single sentence turned into eleven chapters: *the data never moves \u2014 only the camera does*, and everything that surprises a user follows from that. Chapter 1 delivers the hook (one keyword turns a disc face-on or edge-on); Chapter 2 establishes the one mental model everything else hangs on (orthographic parallel rays, hence no camera distance, hence framing must be a field of view); Chapters 3\u20134 are the two decisions that actually cost people figures \u2014 which view specifier and, far more dangerously, world-space ranges versus a camera-plane `fov`; Chapters 5\u20136 separate the guarantee (the total is conserved at any angle, pixel size and binning) from what is *not* guaranteed (placement), in that order, so the recommendation follows its justification instead of preceding it by ninety lines; Chapters 7\u20139 add the three things you build on top of a view \u2014 line-of-sight kinematics, a cutting plane, an orbit sweep \u2014 each defined against what came before rather than cold; Chapters 10\u201311 are debugging and other data types. Three appendices are hard-capped: one limitations table, one pointer to where the guarantees are measured, and one non-executed recipe for keeping a time series from tumbling. The budget is a contract, not an aspiration: one dataset, 13 code cells, 20 hydro `projection` calls, 9 figures, and a measured wall clock stated in Chapter 0.",
   "title": "Off-axis Projection: pointing the camera anywhere"
  }
 }
}